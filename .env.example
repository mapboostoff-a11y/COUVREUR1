# Server Configuration
PORT=3001

# Database Configuration (LibSQL/Turso)
# Leave empty for local SQLite (site-data.db)
# Required for persistent production on Vercel
LIBSQL_DB_URL=libsql://your-database-name.turso.io
LIBSQL_DB_AUTH_TOKEN=your-auth-token

# Environment (optional)
# VERCEL=0
# VERCEL_ENV=development
